import * as React from 'react';
import type { RRuleValue } from '../types';
interface RulePreviewProps {
    value: RRuleValue;
}
export declare const RulePreview: ({ value }: RulePreviewProps) => React.JSX.Element;
export {};
